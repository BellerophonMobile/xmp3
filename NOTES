* Maybe use SASL library?
    * http://www.cyrusimap.org/docs/cyrus-sasl

* Make sure to send proper error codes that the spec says.
* Make sure to work with unicode strings.
* Normalize all Unicode strings received.
* Possibly try to recover from out of memory?
* Clean up header includes
* Use ut* macros for strings
* Recheck all memory allocations

* To generate OpenSSL cert:
    * Private Key:
        openssl genrsa -out server.pem 2048
    * Certificate
        openssl req -new -x509 -key server.pem -out server.cert -days 99999
